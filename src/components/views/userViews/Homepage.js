import TopMenu from "../../user/TopMenu";

const Homepage = () => {
    return (
        <main>
            <TopMenu />
            <div>
                <h2 className="home">Welcome to the homepage!</h2>
            </div>
        </main>
    );
};

export default Homepage;